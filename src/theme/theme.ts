import { createTheme, Theme } from "@mui/material/styles";

const theme: Theme = createTheme({
  palette: {
    primary: { main: "#008484" },
    background: {
      default: "#F6F6F6",
    },
  },
});

export default theme;
